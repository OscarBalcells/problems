#include <bits/stdc++.h>
using namespace std;

using ll = long long int;
using vl = vector<ll>;
using ii = pair<int,int>;


int main() {
    int n, k;
    scanf("%d %d", &n, &k);
    arr.resize(n);
    
    for(int& x : arr) {
        scanf("%d", &x);
    }
}
